[Login]
HandleSuspendKey=ignore
HandleLidSwitch=ignore

Configs
SSH: Port 22 (change to a custom port for security).
SSH: Custom port + key-based auth (configs/ssh/sshd_config).
Lid Suspend: Disabled in /etc/systemd/logind.conf

